---
id: T-004
story: S-001
title: js-render-and-interactivity
type: task
status: open
priority: high
phase: ready
depends_on: [T-002, T-003]
---

## Context

Wire all JavaScript: render functions (food chips, corner cards, story cards, founder grid),
pentagon SVG from week data, day/night toggle reactive updates, bottom sheet open/close,
and all card click handlers with correct sheet content per the Bottom Sheet Inventory in CLAUDE.md.

Pentagon uses the exact `pentaPoints()` formula from CLAUDE.md (angles: -90, -18, 54, 126, 198).
SVG only — no chart library.

## Acceptance Criteria

- renderFood(), renderCorners(), renderStories(), renderFounders() all called on init
- Pentagon renders from week data, prev/next arrows work
- Day/night toggle updates: stat labels, outdoor %, food section title, widget bars
- Every tappable card opens correct bottom sheet per the Bottom Sheet Inventory
- Bottom sheet: slides up 0.25s, overlay darkens, tap outside or ✕ closes
- Corner themes render correctly (glow neon circle + UV label, plants 🌿, kids 🎨, CTA dashed)
- Glow circle has neon-pulse animation
- All 11 bottom sheet configs implemented
