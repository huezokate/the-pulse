# Plan — T-001: foundation-css-shell

## Steps

1. Create index.html with head, fonts, full :root CSS vars, reset
2. Add all component CSS (ordered by section flow)
3. Add animations (@keyframes pulse-dot, glow-pulse)
4. Add bottom sheet HTML (overlay + sheet DOM)
5. Add empty app shell with section placeholders
6. Add fixed bottom nav HTML + CSS

## Verification

- Open index.html in browser — page background is honied white (#F2E8D5)
- Font loads — DM Serif Display renders correctly on a heading
- Bottom sheet hidden by default, no layout shift
- Bottom nav appears at bottom of viewport, frosted
